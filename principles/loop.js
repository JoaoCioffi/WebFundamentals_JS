// with var
for (var i=0; i<16; i++){
    console.log(`sqrt(${i}) = ${(i**0.5).toFixed(3)}`)
}
console.log('i = ',i)

// with let
for (let x=0; i<16; i++){
    console.log(`sqrt(${x}) = ${(x**0.5).toFixed(3)}`)
}
console.log('x = ',x)
